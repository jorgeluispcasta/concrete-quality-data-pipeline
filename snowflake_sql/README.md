# Snowflake SQL

The SQL layer creates:

- raw landing tables
- a small star schema
- operational analytics views
- acquisition candidate views
- validation queries

Run files `01` through `06` in order. File `07` is an optional AWS/S3 deployment pattern.
