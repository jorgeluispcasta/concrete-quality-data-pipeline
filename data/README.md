# Data

The raw CSV files are synthetic and generated by `data_simulation/run_all.py`.

They represent a fictional concrete manufacturing network and are included in the repository so the project can be explored without access to proprietary company data.

To regenerate them:

```bash
python data_simulation/run_all.py
```
